# 作業計画: 月報アプリ改修（宿題①②実装）
作成日時: 2026-06-23 17:32:48

## 計画（チェックリスト）

### フェーズ1: スキーマ追加（Setup.gs）― 全後続の土台

- [x] 1-1: Setup.gs の SHEET_OCR ヘッダー末尾に `個数` `走行距離(km)` `修正後個数` `修正後走行距離` を追加（→ 列[13][14][15][16]）
- [x] 1-2: Setup.gs の SHEET_CONFIRM ヘッダー末尾に `個数合計` `走行距離合計(km)` `超過km合計` を追加（→ 列[11][12][13]）
- [ ] 1-3: GAS エディタで setupSheets() を手動実行し、シートのヘッダー行（1行目）が更新されること・既存データ行（2行目以降）が無変化であることを確認

### フェーズ2: バックエンド計算（Dashboard.gs）

- [x] 2-1: `handleAdminGetOcrDetail_` — 日別レスポンスに `kosu`・`distance`・`overKm`（= max(0, distance−100)）を追加
- [x] 2-2: `handleAdminSaveCorrection_` — payloadに `fixedKosu`・`fixedDistance` を受け取り、シートの col16・col17（= [15][16]）に書き込む処理を追加。操作ログ行も拡張
- [x] 2-3: `handleAdminConfirmMonth_` — 個数合計・走行距離合計・超過km合計（= Σ max(0, 日別距離−100)）を算出し、月次確定シートの col12・col13・col14（= [11][12][13]）に書き込む。日別距離は [16]修正後 → [14]OCR の優先順
- [x] 2-4: `handleAdminGetDriverList_` — 一覧レスポンスの各ドライバー行に `totalDistance`・`totalOverKm`（・`totalKosu`）を付与
- [x] 2-5: `handleAdminGetOverview_` — 統計カードレスポンスに当月の超過km合計を追加
- [x] 2-6: `handleAdminExportData_` — CSV の列に `個数合計`・`走行距離合計(km)`・`超過km合計` を追加

### フェーズ3: フロントエンド表示（staff/）

- [x] 3-1: `staff/css/dashboard.css` — 100km 超過日ハイライト用スタイル追加（例: `.over-km-row { background: #fff3cd; }` など）
- [x] 3-2: `staff/js/dashboard.js` — 詳細画面の日別テーブルに「個数」「走行距離」入力セルと「超過km」表示列を追加
- [x] 3-3: `staff/js/dashboard.js` — 100km 超過日に over-km-row クラスを付与するハイライトロジック追加
- [x] 3-4: `staff/js/dashboard.js` — 一覧画面の各行に走行距離合計・超過km合計を表示
- [x] 3-5: `staff/js/dashboard.js` — 統計カードに超過km合計を表示
- [x] 3-6: `staff/index.html` — 新規列ヘッダー・カード要素を追加し、`?v=` を更新

### フェーズ4: ①一次チェック（docs/js/app.js・docs/index.html）

- [x] 4-1: `docs/js/app.js` 先頭にファイルチェック閾値定数を定義（MIN_LONG_EDGE, BRIGHTNESS_MIN/MAX, BLUR_VAR_MIN）
- [x] 4-2: `docs/js/app.js` に `checkImageQuality_(file)` 関数を実装（デコード可否 → 解像度 → 明るさ → ブラー の順で判定。各 NG は { ok: false, reason, message } を返す。警告でも続行できる設計）
- [x] 4-3: `docs/js/app.js` の `splitForOcr` 前段に `checkImageQuality_` の呼び出しを追加。NG 時はフィードバック UI を表示し「このまま提出」フォールバックを提供
- [x] 4-4: `docs/index.html` にフィードバック表示要素（警告メッセージ + 「このまま提出」ボタン）を追加。`?v=` 更新

### フェーズ5: ①二次チェック（OcrService.gs・Code.gs）

- [x] 5-1: `OcrService.gs` に月報らしさ判定関数 `runReportValidation_(base64)` を追加。返す情報は `{ isReport, canRead, reason }` のみの軽量プロンプト（既存 DAYS/META 系とは別）
- [x] 5-2: `Code.gs` の `handleUploadReport_` 内 `runOcr` 呼び出し前に `runReportValidation_` を挿入。判定 NG はレスポンスに `{ validationWarning, validationReason }` を追加（ブロックではなく警告）。フロント側（`app.js`）で警告表示 + フォールバック（このまま提出）を処理

### フェーズ6: デプロイ

- [ ] 6-1: GAS エディタで「デプロイを管理」→ 新バージョンでデプロイ
- [ ] 6-2: `git push origin main`（GitHub Pages 自動更新）
- [ ] 6-3: 全 HTML・`config.js` の `?v=` が最新日付になっているか最終確認

---

## レビュー指摘事項（実装時に適用）

- 計画書 §3-2「アップロードフローの同期化が必要」は誤記。GAS はシングルスレッドのため現状すでに同期的。フェーズ5 は同期化不要で、`runOcr` 前に判定関数を呼ぶだけでよい。
- `handleAdminSaveCorrection_` は列番号をハードコード（数値リテラル）。フェーズ2-2 では `sheet.getRange(i+1, 16)` / `sheet.getRange(i+1, 17)` の書き込みを明示的に追加する。

## 結果＆発見事項

- 1-1/1-2: Node.js スキーマテスト（tests/schema.test.js）で RED→GREEN を確認。Setup.gs に新7列追加完了。
- 2-1〜2-6: Node.js テスト計32件 (schema/calc/ocrDetail/saveCorrection/confirmMonth) 全パス。Dashboard.gs 6関数拡張完了。
- 発見: テスト作成中に totalDistance/totalKosu の期待値に算数ミスがあり、テストが実装の正しさを証明する役割を果たした。
- 3-1〜3-6: フロントエンド計算テスト10件（frontendCalc.test.js）全パス。dashboard.js/index.html/dashboard.css 更新完了。全スイート 42件パス。
- 4-1〜4-4: 画像品質チェック実装済み。imageCheck.test.js 10件パス。app.js + index.html 更新完了。全スイート 52件パス。
- 5-1〜5-2: 月報らしさ二次チェック実装済み。reportValidation.test.js 7件 + validationDisplay.test.js 6件パス。OcrService.gs + Code.gs + app.js 更新完了。全スイート 65件パス。

## 問題
（問題が発生したら、詳細をここに追記する）
